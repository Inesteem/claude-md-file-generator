---
name: Linter Configuration
type: template
tags: [linting, tooling]
order: 23
description: Project-specific linter setup and commands (agent-filled and verified)
---
<Fill Linter Configuration>
Identify the linter(s) used in this project and document:
- Which linter(s) are configured (e.g. ruff, eslint, pylint, clippy, golangci-lint)
- The exact command to run them
- Where the configuration lives (e.g. pyproject.toml, .eslintrc, .ruff.toml)
- Any notable rules that are enabled, disabled, or customized
- How to auto-fix lint issues (if supported)

Verify: run the lint command and confirm it works. If it fails, note what is broken.
</Fill Linter Configuration>
