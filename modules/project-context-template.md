---
name: Project Context
type: template
tags: [overview, setup]
order: 2
description: Project setup, commands, and environment requirements (agent-filled)
---
<Project Context>
Fill in the following for this project:
- Setup commands (e.g. `pip install -e ".[dev]"`, `npm install`)
- How to run the project locally
- How to run tests and linting
- Key dependencies and why each one exists
- Required environment variables or configuration
- Any non-obvious environment requirements (Python version, system libs, etc.)
</Project Context>
