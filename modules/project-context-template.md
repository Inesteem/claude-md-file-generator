---
name: Project Context
type: template
tags: [overview, setup]
order: 2
description: Project setup, commands, and environment requirements (agent-filled)
---
<Project Context>
Fill in the following for this project:
- Setup commands (e.g. `pip install -e ".[dev]"`, `npm install`)
- How to run the project locally
- How to run tests (command, framework, any flags that matter)
- How to run linting and formatting (command, tool names)
- Required environment variables or configuration (with example values where safe)
- Non-obvious environment requirements (specific runtime versions, system libraries, OS constraints)
- Critical or non-obvious dependencies and why they exist (skip anything self-explanatory)
</Project Context>
